// Lógica do jogo
let jogo = new Phaser.Game({
  width: 800,
  height: 600,
  scene: {
    preload: preload,
    create: create,
    update: update
  }
});

function preload() {
  // Carregar recursos
}

function create() {
  // Criar a torre e os inimigos
}

function update() {
  // Atualizar a lógica do jogo
}
